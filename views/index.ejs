<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
     <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            
                <form action="/create" method="post">
                <input  class="block px-5 py-3 bg-zinc-800  w-full rounded-md outline-none"   type="text" placeholder="Your Title" name="title">
                <textarea  class="block px-5 py-3 bg-zinc-800 resize-none  mt-3  w-full rounded-md outline-none"    placeholder=" Write Your Task Here.."  name="details"></textarea>
                <input   class="mt-3 bg-red-500 px-4 py-2 rounded-md"   type="submit"  value="Create Task">
            </form>
        </div>
        <div class="tasks    p-10 flex gap-3 flex-wrap ">

            <% if(files.length >0){ %>


                <% files.forEach(function(val){ %>

                    <div class="task  min-w-72 px-3 py-4 bg-zinc-800 rounded-md">
                        <h1 class="text-2xl text-white "><%= val %></h1>
                        <a   class="text-red-500 font-semibold inline-block mt-3"  href="/file/<%= val %>">Read More..</a>
                        <a    class=" ml-10 text-blue-500"  href="/edit/<%= val %>"> Edit</a>
                        <a    class=" ml-10 text-fuchsia-700"  href="/delete/<%= val %>"> Delete</a>
                       
                    </div>

                    <% })%>

                     <% }else { %>
                        <h1 class="text-zinc-600">no task yet..</h1>

           <%  } %>

             

           
          
        </div>
      
     </div>
</body>
</html>